version: "3.8"
services:
  app:
    container_name: oas-platform-hotels-master-api
    build:
      context: .
      dockerfile: ./build/Dockerfile.local
      args:
        ACCESS_TOKEN: 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
    volumes:
      - ./:/app/
    working_dir: /app
    env_file:
      - .env
    ports:
      - "${APP_PORT}:${APP_PORT}"
